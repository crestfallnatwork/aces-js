{
  "name": "acess-js",
  "version": "0.5.0",
  "description": "Node.JS implementation of aptos chain encryption scheme which is a way to do e2e encrypt messages on aptos and more",
  "scripts": {
    "test": "tsx --test",
    "build": "esbuild index.ts --bundle --outfile=dist/out.js --platform=browser --external:node:crypto"
  },
  "author": "crestfalln",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.10",
    "esbuild": "0.23.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "@aptos-labs/ts-sdk": "^1.22.2",
    "@noble/curves": "^1.4.2"
  }
}
